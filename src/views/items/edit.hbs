<div class="container p-4">
    <div class="row">
        <div class="col-md-3 mx-auto">
            <div class="card">
                <div class="card-body">
                    <form action="/items/edit/{{item.id}}" method="POST">
                        <div class="form-group">
                            <input type="text" value="{{item.title}}" class="form-control mb-3" name="title" placeholder="Título" required autofocus>
                            {{#if item.done}}
                                <input type="checkbox" name="done" checked>
                                Hecho
                            {{else}}
                                <input type="checkbox" name="done">
                                Hecho
                            {{/if}}
                        </div>
                        <div class="mt-3">
                            <button class="btn btn-success w-100">
                                Guardar
                            </button>
                        </div>
                        <div>
                            <a href="/items" class="btn btn-secondary w-100 mt-3">
                                Cancelar
                            </a>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>